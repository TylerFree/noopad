# Nopad

Nopad is a lightweight cross-platform engineer text editor built with Avalonia.

## Run

Install the .NET 8 SDK, then run:

```powershell
dotnet restore
dotnet run
```

This workspace also has a local SDK copy available under `.dotnet` if `dotnet` is not on PATH:

```powershell
.\.dotnet\dotnet.exe run
```
